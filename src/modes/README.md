# modes/ (placeholder)

Reserved for per-mode rule-set modules (main progression mode, mining
mode, PvP zone, build/test lab, squad mode, exploration mode, short
high-intensity mode, private sessions). The current build only
implements the shared core loop (free-roam exploration + mining +
combat), not distinct mode rule-sets yet.
